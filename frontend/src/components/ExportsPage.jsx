import Icon from './Icon'

export default function ExportsPage({ programs, onOpen }) {
  return <div className="mx-auto max-w-[1300px] px-4 py-7 sm:px-6 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-blue-700">Centro de exportación</p><h2 className="mt-1 text-2xl font-bold tracking-tight text-slate-950">Exportaciones</h2><p className="mt-2 text-sm text-slate-500">Previsualiza las hojas que corresponden a cada tipo de programa.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{programs.map((program) => { const flexible = program.jurisdiction === 'Federal' && ['Mixta', 'No escolarizada'].includes(program.modality); return <article key={program.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start gap-4"><span className="grid h-11 w-11 place-items-center rounded-xl bg-emerald-100 text-emerald-700"><Icon name="file" size={20} /></span><div className="min-w-0 flex-1"><h3 className="truncate text-sm font-bold text-slate-900">{program.name}</h3><p className="mt-1 text-[10px] text-slate-400">{program.plan} · {program.jurisdiction} · {program.modality}</p><div className="mt-3 flex flex-wrap gap-1.5"><span className="rounded-md bg-slate-100 px-2 py-1 text-[9px] font-semibold text-slate-600">Mapa curricular</span><span className={`rounded-md px-2 py-1 text-[9px] font-semibold ${flexible ? 'bg-blue-100 text-blue-700' : 'bg-amber-100 text-amber-700'}`}>{flexible ? 'Flexible' : 'Optativas'}</span></div></div></div><button type="button" onClick={() => onOpen(program)} className="mt-5 flex h-9 w-full items-center justify-center gap-2 rounded-xl border border-slate-200 text-xs font-semibold text-slate-700 hover:border-blue-300 hover:bg-blue-50 hover:text-blue-700"><Icon name="export" size={14} /> Configurar exportación</button></article>})}</div></div>
}
